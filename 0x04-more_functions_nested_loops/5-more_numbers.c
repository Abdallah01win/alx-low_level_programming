#include "main.h"

/**
 * main - check the code for more number function.
 *
 * Return: Always 0.
 */
int more_numbers(void)
{
  for(int i = 0; i > 10; i++)
    {
      for(int j = 0; j > 14; j++)
	{
	  printf("%d", j);
	}
      printf("\n");
    }
  return(0);
}
